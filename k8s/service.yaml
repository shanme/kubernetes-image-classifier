apiVersion: v1
kind: Service
metadata:
  name: image-classifier-service
  labels:   
    app: image-classifier
spec:
  selector:
    app: image-classifier  
  ports:
    - name: http
      protocol: TCP
      port: 80         
      targetPort: 4123    
  type: LoadBalancer